<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0">
    <title>Contact us · Help with Bailiffs</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:opsz,wght@14..32,400;14..32,500;14..32,600;14..32,700&display=swap" rel="stylesheet">
    <style>
        /* same base styles as about.html */
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Inter', sans-serif; background: #f4f7fc; color: #1e2b3c; line-height: 1.5; min-height: 100vh; display: flex; flex-direction: column; }
        .page-wrap { max-width: 1280px; margin: 0 auto; padding: 0 1rem 2rem; width: 100%; flex: 1; }

        .sticky-nav { position: sticky; top: 12px; z-index: 1000; width: 100%; display: flex; justify-content: flex-end; margin-bottom: 1rem; }
        .hamburger-btn { background: rgba(255,255,255,0.98); backdrop-filter: blur(8px); border: 1px solid rgba(221,233,245,0.8); border-radius: 60px; padding: 0.7rem 1.5rem; box-shadow: 0 8px 20px rgba(0,20,40,0.12); cursor: pointer; display: inline-flex; align-items: center; gap: 10px; font-size: 1.2rem; font-weight: 600; color: #1f4b7a; }
        .hamburger-btn i { font-size: 1.6rem; color: #2f6eb3; }
        .hamburger-btn.active { background: #1f4b7a; color: white; }
        .hamburger-btn.active i { color: white; }

        .nav-menu { position: absolute; top: calc(100% + 8px); right: 0; width: 240px; background: rgba(255,255,255,0.98); backdrop-filter: blur(8px); border: 1px solid #dde9f5; border-radius: 28px; padding: 1rem 0; display: none; flex-direction: column; z-index: 1001; }
        .nav-menu.show { display: flex; }
        .nav-menu a { color: #1f4b7a; text-decoration: none; font-weight: 600; padding: 0.9rem 1.8rem; border-bottom: 1px solid #eef2f6; display: flex; align-items: center; gap: 12px; }
        .nav-menu a i { width: 24px; color: #2f6eb3; }

        .desktop-nav { display: none; flex-wrap: wrap; justify-content: center; gap: 1.5rem 2.5rem; background: white; padding: 1rem 2rem; border-radius: 60px; margin: 1rem 0 2rem; border: 1px solid #dde9f5; }
        .desktop-nav a { color: #1f4b7a; text-decoration: none; font-weight: 600; }
        .desktop-nav a i { margin-right: 6px; color: #2f6eb3; }
        @media (min-width: 700px) { .sticky-nav { display: none; } .desktop-nav { display: flex; } }

        .site-header { text-align: center; margin: 0.5rem 0 2rem; }
        .site-header h1 { font-size: 2rem; font-weight: 700; background: linear-gradient(145deg, #0b2b4f, #1f4b7a); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
        .site-header h1 i { -webkit-text-fill-color: #1f4b7a; }
        .tagline { font-size: 1.1rem; font-weight: 500; color: #2c3e5c; border-bottom: 2px solid #d0e0f0; padding-bottom: 0.75rem; }

        .card { background: white; border-radius: 28px; padding: 2rem; box-shadow: 0 12px 30px rgba(0,20,50,0.08); border: 1px solid #eef4fa; max-width: 700px; margin: 0 auto; }
        .card h2 { font-size: 1.8rem; color: #0c2f51; margin-bottom: 1.5rem; }
        .card h2 i { color: #2f6eb3; margin-right: 10px; }

        /* form styles */
        .form-group { margin-bottom: 1.5rem; }
        label { font-weight: 600; display: block; margin-bottom: 0.3rem; color: #1e3d5e; }
        input, textarea { width: 100%; padding: 0.9rem 1.2rem; border: 1.5px solid #cbd6e4; border-radius: 50px; font-size: 1rem; background: #f9fcff; }
        textarea { border-radius: 24px; resize: vertical; }
        button[type="submit"] { background: #1c4e7c; color: white; border: none; width: 100%; padding: 1.1rem; font-size: 1.2rem; font-weight: 700; border-radius: 60px; cursor: pointer; }
        .privacy-note { font-size: 0.85rem; background: #f2f8ff; padding: 1rem; border-radius: 18px; margin: 1.2rem 0; }

        .footer-nav { margin-top: 3rem; padding-top: 1.5rem; border-top: 2px solid #d0e0f0; display: flex; flex-wrap: wrap; justify-content: center; gap: 1rem 1.8rem; }
        .footer-nav a { color: #1f4b7a; text-decoration: none; font-weight: 600; }
        .copyright { text-align: center; margin-top: 1rem; color: #3f6286; font-size: 0.85rem; }
        .text-small { font-size: 0.9rem; color: #3b5f85; }
    </style>
</head>
<body>
    <div class="page-wrap">
        <div class="sticky-nav">
            <div class="hamburger-btn" id="hamburgerBtn"><i class="fas fa-bars"></i> <span>Menu</span></div>
            <div class="nav-menu" id="navMenu">
                <a href="index.html"><i class="fa-solid fa-house"></i> Home</a>
                <a href="about.html"><i class="fa-regular fa-address-card"></i> About</a>
                <a href="contact.html"><i class="fa-regular fa-envelope"></i> Contact</a>
                <a href="terms.html"><i class="fa-regular fa-file-lines"></i> Terms</a>
                <a href="privacy.html"><i class="fa-regular fa-lock"></i> Privacy</a>
            </div>
        </div>
        <div class="desktop-nav">
            <a href="index.html"><i class="fa-solid fa-house"></i> Home</a>
            <a href="about.html"><i class="fa-regular fa-address-card"></i> About</a>
            <a href="contact.html"><i class="fa-regular fa-envelope"></i> Contact</a>
            <a href="terms.html"><i class="fa-regular fa-file-lines"></i> Terms</a>
            <a href="privacy.html"><i class="fa-regular fa-lock"></i> Privacy</a>
        </div>

        <div class="site-header">
            <h1><i class="fas fa-shield-halved"></i> Help with Bailiffs</h1>
            <div class="tagline">Contact us</div>
        </div>

        <div class="card">
            <h2><i class="fa-regular fa-envelope"></i> Send a message</h2>
            <form action="https://api.web3forms.com/submit" method="POST">
                <input type="hidden" name="access_key" value="1b7a8d1e-8c3f-4b2d-9a6f-5e3c2b1d8f9a">
                <input type="hidden" name="subject" value="New contact message from Help Bailiffs">
                
                <div class="form-group">
                    <label>Full name *</label>
                    <input type="text" name="name" required placeholder="John Miller">
                </div>
                <div class="form-group">
                    <label>Email address *</label>
                    <input type="email" name="email" required placeholder="you@example.co.uk">
                </div>
                <div class="form-group">
                    <label>Phone number (optional)</label>
                    <input type="tel" name="phone" placeholder="0203 996 3438">
                </div>
                <div class="form-group">
                    <label>Message *</label>
                    <textarea name="message" rows="4" required placeholder="How can we help?"></textarea>
                </div>

                <div class="privacy-note">
                    <i class="fas fa-lock"></i> By submitting you agree to our privacy policy. We'll only use your details to reply.
                </div>

                <input type="checkbox" name="botcheck" style="display: none;">
                <button type="submit">Send message</button>
            </form>
            <p class="text-small" style="margin-top:1.5rem;">📞 Or call: <strong>0203 996 3438</strong> (Mon-Fri 8am-8pm)</p>
        </div>

        <footer>
            <div class="footer-nav">
                <a href="about.html">About</a> <a href="terms.html">Terms</a> <a href="privacy.html">Privacy</a> <a href="contact.html">Contact</a>
            </div>
            <div class="copyright">© 2025 Help with Bailiffs</div>
        </footer>
    </div>

    <script>
        (function(){
            const btn = document.getElementById('hamburgerBtn');
            const menu = document.getElementById('navMenu');
            function toggle(){
                menu.classList.toggle('show');
                btn.classList.toggle('active');
                const icon = btn.querySelector('i');
                const span = btn.querySelector('span');
                if(menu.classList.contains('show')){ icon.className = 'fas fa-times'; span.textContent = 'Close'; }
                else { icon.className = 'fas fa-bars'; span.textContent = 'Menu'; }
            }
            btn.addEventListener('click', (e) => { e.stopPropagation(); toggle(); });
            menu.querySelectorAll('a').forEach(link => link.addEventListener('click', () => { if(window.innerWidth<=699) toggle(); }));
            document.addEventListener('click', (e) => { if(window.innerWidth<=699 && !btn.contains(e.target) && !menu.contains(e.target) && menu.classList.contains('show')) toggle(); });
        })();
    </script>
</body>
</html>
