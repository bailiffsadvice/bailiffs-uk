<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0">
    <title>Contact us · Help with Bailiffs</title>
    <!-- Font Awesome 6 (free) -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <!-- Google Fonts: Inter -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:opsz,wght@14..32,400;14..32,500;14..32,600;14..32,700&display=swap" rel="stylesheet">
    <style>
        /* === RESET & GLOBAL === */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: #f4f7fc;
            color: #1e2b3c;
            line-height: 1.5;
            scroll-behavior: smooth;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .page-wrap {
            max-width: 1280px;
            margin: 0 auto;
            padding: 0 1rem 2rem;
            width: 100%;
            flex: 1;
        }

        /* === STICKY HAMBURGER NAV (mobile first) === */
        .sticky-nav {
            position: sticky;
            top: 12px;
            z-index: 1000;
            width: 100%;
            display: flex;
            justify-content: flex-end;
            margin-bottom: 1rem;
        }

        .hamburger-btn {
            background: rgba(255, 255, 255, 0.98);
            backdrop-filter: blur(8px);
            -webkit-backdrop-filter: blur(8px);
            border: 1px solid rgba(221, 233, 245, 0.8);
            border-radius: 60px;
            padding: 0.7rem 1.5rem;
            box-shadow: 0 8px 20px rgba(0,20,40,0.12);
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            gap: 10px;
            font-size: 1.2rem;
            font-weight: 600;
            color: #1f4b7a;
            touch-action: manipulation;
            transition: 0.2s;
        }

        .hamburger-btn i {
            font-size: 1.6rem;
            color: #2f6eb3;
        }

        .hamburger-btn.active {
            background: #1f4b7a;
            color: white;
        }
        .hamburger-btn.active i {
            color: white;
        }

        .nav-menu {
            position: absolute;
            top: calc(100% + 8px);
            right: 0;
            width: 240px;
            background: rgba(255, 255, 255, 0.98);
            backdrop-filter: blur(8px);
            -webkit-backdrop-filter: blur(8px);
            border: 1px solid #dde9f5;
            border-radius: 28px;
            padding: 1rem 0;
            box-shadow: 0 16px 30px rgba(0,20,40,0.16);
            display: none;
            flex-direction: column;
            z-index: 1001;
        }

        .nav-menu.show {
            display: flex;
        }

        .nav-menu a {
            color: #1f4b7a;
            text-decoration: none;
            font-weight: 600;
            font-size: 1.1rem;
            padding: 0.9rem 1.8rem;
            border-bottom: 1px solid #eef2f6;
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .nav-menu a:last-child {
            border-bottom: none;
        }

        .nav-menu a i {
            width: 24px;
            color: #2f6eb3;
            font-size: 1.2rem;
        }

        .nav-menu a:hover {
            background: #e3efff;
        }

        /* desktop nav */
        .desktop-nav {
            display: none;
            flex-wrap: wrap;
            justify-content: center;
            gap: 1.5rem 2.5rem;
            background: white;
            padding: 1rem 2rem;
            border-radius: 60px;
            margin: 1rem 0 2rem;
            border: 1px solid #dde9f5;
        }

        .desktop-nav a {
            color: #1f4b7a;
            text-decoration: none;
            font-weight: 600;
            font-size: 1.1rem;
        }

        .desktop-nav a i {
            margin-right: 6px;
            color: #2f6eb3;
        }

        @media (min-width: 700px) {
            .sticky-nav {
                display: none;
            }
            .desktop-nav {
                display: flex;
            }
        }

        /* header */
        .site-header {
            text-align: center;
            margin: 0.5rem 0 1.5rem;
        }

        .site-header h1 {
            font-size: 2rem;
            font-weight: 700;
            background: linear-gradient(145deg, #0b2b4f, #1f4b7a);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            letter-spacing: -0.02em;
            line-height: 1.2;
        }

        .site-header h1 i {
            -webkit-text-fill-color: #1f4b7a;
            margin-right: 6px;
        }

        .tagline {
            font-size: 1.1rem;
            font-weight: 500;
            color: #2c3e5c;
            border-bottom: 2px solid #d0e0f0;
            padding-bottom: 0.75rem;
            margin-top: 0.4rem;
        }

        /* cards */
        .card {
            background: white;
            border-radius: 28px;
            padding: 1.5rem 1.2rem;
            box-shadow: 0 8px 24px rgba(0,20,50,0.06);
            margin-bottom: 1.5rem;
            border: 1px solid #eef4fa;
        }

        .card h2 {
            font-size: 1.6rem;
            font-weight: 600;
            color: #0c2f51;
            margin-bottom: 1rem;
        }

        .card h2 i {
            color: #2f6eb3;
            margin-right: 8px;
        }

        .card h3 {
            font-size: 1.3rem;
            font-weight: 600;
            margin: 1rem 0 0.6rem;
            color: #1f3a5f;
        }

        /* two column layout for contact page */
        .contact-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 1.5rem;
        }

        @media (min-width: 800px) {
            .contact-grid {
                grid-template-columns: 1fr 1fr;
            }
        }

        /* info panel (about stopping bailiffs) */
        .info-panel {
            background: linear-gradient(145deg, #e8f0fe, #ffffff);
            border-radius: 28px;
            padding: 1.8rem;
            height: fit-content;
            border: 1px solid #c9ddf5;
        }

        .info-panel h2 {
            color: #0b2b4f;
            font-size: 1.8rem;
            margin-bottom: 1.5rem;
            border-left: 6px solid #c44536;
            padding-left: 1rem;
        }

        .info-panel h3 {
            color: #1f4b7a;
            margin: 1.5rem 0 0.8rem;
            font-size: 1.3rem;
        }

        .info-panel p {
            margin-bottom: 1rem;
            color: #2b4b6e;
        }

        .info-panel ul {
            margin: 1rem 0;
            padding-left: 1.5rem;
        }

        .info-panel li {
            margin-bottom: 0.6rem;
            color: #2b4b6e;
        }

        .contact-details {
            background: white;
            border-radius: 20px;
            padding: 1.5rem;
            margin-top: 1.5rem;
            border: 1px solid #d0e0f0;
        }

        .contact-details p {
            margin: 0.8rem 0;
            font-size: 1.1rem;
        }

        .contact-details i {
            color: #2f6eb3;
            width: 30px;
            font-size: 1.3rem;
        }

        /* form styles (same as index) */
        .contact-form {
            background: white;
            border-radius: 32px;
            padding: 2rem 1.5rem;
            box-shadow: 0 16px 28px rgba(0,32,64,0.1);
            border: 1px solid #d6e3f0;
        }

        .form-step {
            font-size: 0.85rem;
            text-transform: uppercase;
            color: #51718c;
            margin-bottom: 0.8rem;
        }

        .form-group {
            margin-bottom: 1.3rem;
        }

        label {
            font-weight: 600;
            display: block;
            margin-bottom: 0.3rem;
            color: #1e3d5e;
            font-size: 0.95rem;
        }

        input, select, textarea {
            width: 100%;
            padding: 0.9rem 1.2rem;
            border: 1.5px solid #cbd6e4;
            border-radius: 50px;
            font-size: 1rem;
            background: #f9fcff;
        }

        textarea {
            border-radius: 24px;
            resize: vertical;
        }

        .privacy-note {
            font-size: 0.8rem;
            background: #f2f8ff;
            padding: 1rem;
            border-radius: 18px;
            margin: 1.2rem 0;
        }

        button[type="submit"] {
            background: #1c4e7c;
            color: white;
            border: none;
            width: 100%;
            padding: 1.1rem;
            font-size: 1.2rem;
            font-weight: 700;
            border-radius: 60px;
            cursor: pointer;
            transition: 0.15s;
        }

        button[type="submit"]:hover {
            background: #0f3a5f;
        }

        button[type="submit"]:disabled {
            background: #95b3cf;
            cursor: not-allowed;
        }

        /* thank you message */
        .thankyou-message {
            background: #e2f0da;
            border-left: 6px solid #2e7d32;
            border-radius: 20px;
            padding: 1.5rem;
            margin: 1rem 0;
            text-align: center;
        }

        .thankyou-message i {
            font-size: 3rem;
            color: #2e7d32;
            margin-bottom: 0.5rem;
        }

        .thankyou-message h3 {
            font-size: 1.8rem;
            color: #1e4a2a;
            margin-bottom: 0.5rem;
        }

        .hidden {
            display: none;
        }

        /* footer */
        .footer-nav {
            margin-top: 2.5rem;
            padding-top: 1.5rem;
            border-top: 2px solid #d0e0f0;
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 1rem 1.8rem;
        }

        .footer-nav a {
            color: #1f4b7a;
            text-decoration: none;
            font-weight: 600;
            font-size: 1rem;
        }

        .copyright {
            text-align: center;
            margin-top: 1rem;
            color: #3f6286;
            font-size: 0.85rem;
        }

        .text-small {
            font-size: 0.85rem;
            color: #3b5f85;
        }
        .mt-2 { margin-top: 1rem; }
        .text-center { text-align: center; }
    </style>
</head>
<body>
    <div class="page-wrap">
        <!-- ========== HAMBURGER MENU ========== -->
        <div class="sticky-nav">
            <div class="hamburger-btn" id="hamburgerBtn">
                <i class="fas fa-bars"></i> <span>Menu</span>
            </div>
            <div class="nav-menu" id="navMenu">
                <a href="index.html"><i class="fa-solid fa-house"></i> Home</a>
                <a href="about.html"><i class="fa-regular fa-address-card"></i> About</a>
                <a href="contact.html"><i class="fa-regular fa-envelope"></i> Contact</a>
                <a href="terms.html"><i class="fa-regular fa-file-lines"></i> Terms</a>
                <a href="privacy.html"><i class="fa-regular fa-lock"></i> Privacy</a>
            </div>
        </div>

        <!-- ========== DESKTOP NAV ========== -->
        <div class="desktop-nav">
            <a href="index.html"><i class="fa-solid fa-house"></i> Home</a>
            <a href="about.html"><i class="fa-regular fa-address-card"></i> About</a>
            <a href="contact.html"><i class="fa-regular fa-envelope"></i> Contact</a>
            <a href="terms.html"><i class="fa-regular fa-file-lines"></i> Terms</a>
            <a href="privacy.html"><i class="fa-regular fa-lock"></i> Privacy</a>
        </div>

        <!-- ========== HEADER ========== -->
        <div class="site-header">
            <h1><i class="fas fa-shield-halved"></i> Help with Bailiffs</h1>
            <div class="tagline">Contact us – free advice & support</div>
        </div>

        <!-- ========== TWO-COLUMN CONTACT LAYOUT ========== -->
        <div class="contact-grid">
            <!-- LEFT COLUMN: Info about stopping bailiffs + address/phone -->
            <div class="info-panel">
                <h2><i class="fas fa-hand"></i> Stopping the Bailiffs</h2>
                
                <p><strong>Facing bailiff action can be intimidating, but you have rights and options.</strong> Our team specialises in helping people like you deal with enforcement agents and find lasting solutions.</p>
                
                <h3><i class="fas fa-shield-alt"></i> Key steps to stop bailiffs:</h3>
                <ul>
                    <li><i class="fas fa-check-circle" style="color:#2e7d32;"></i> <strong>Don't ignore the problem</strong> – early advice gives you more options.</li>
                    <li><i class="fas fa-check-circle" style="color:#2e7d32;"></i> <strong>Know what they can and cannot take</strong> – bailiffs have strict rules.</li>
                    <li><i class="fas fa-check-circle" style="color:#2e7d32;"></i> <strong>You don't have to let them in</strong> – communicate through a window or door.</li>
                    <li><i class="fas fa-check-circle" style="color:#2e7d32;"></i> <strong>Seek a repayment plan or formal solution</strong> – like an IVA or payment arrangement.</li>
                </ul>

                <p>We've helped thousands of people across the UK stop bailiff visits, freeze interest, and write off unaffordable debt. Our advisors are here to listen and guide you.</p>

                <!-- Contact details with address and phone -->
                <div class="contact-details">
                    <h3 style="margin-top:0;"><i class="fas fa-address-card"></i> Our office</h3>
                    <p><i class="fas fa-map-pin"></i> 123 High Street, London, EC1A 1BB</p>
                    <p><i class="fas fa-phone-alt"></i> <strong>0203 996 3438</strong> (Mon-Fri, 8am-8pm)</p>
                    <p><i class="fas fa-envelope"></i> advice@helpbailiffs.co.uk</p>
                    <p><i class="fas fa-clock"></i> Office hours: 8am – 8pm, Monday to Friday<br>Weekend emergencies: leave a message</p>
                </div>

                <p style="font-style: italic; margin-top: 1rem;">All calls are confidential. We're a team of debt advisers and former enforcement agents committed to fair treatment.</p>
            </div>

            <!-- RIGHT COLUMN: Contact form (identical to index page fact finder) -->
            <div class="contact-form">
                <h2 style="font-size:1.9rem; margin-bottom:0.2rem;">📋 Get free help</h2>
                <div class="form-step"><i class="far fa-circle-check"></i> Same form as our fact finder</div>
                
                <!-- Thank you message (hidden by default) -->
                <div id="thankyouMessage" class="thankyou-message hidden">
                    <i class="fas fa-check-circle"></i>
                    <h3>Thank you!</h3>
                    <p>Your message has been sent. One of our advisors will contact you shortly.</p>
                </div>
                
                <form id="contactForm">
                    <input type="hidden" name="access_key" value="1b7a8d1e-8c3f-4b2d-9a6f-5e3c2b1d8f9a">
                    <input type="hidden" name="subject" value="New Contact Form from Help Bailiffs">
                    
                    <div class="form-group">
                        <label>Full name *</label>
                        <input type="text" name="name" required placeholder="e.g. John Miller">
                    </div>

                    <div class="form-group">
                        <label>Email address *</label>
                        <input type="email" name="email" required placeholder="you@example.co.uk">
                    </div>

                    <div class="form-group">
                        <label>Phone number *</label>
                        <input type="tel" name="phone" required placeholder="0203 996 3438">
                        <p class="text-small" style="margin-top:4px;">We'll contact you on this number.</p>
                    </div>

                    <!-- Bailiff (if known) optional -->
                    <div class="form-group">
                        <label>Bailiff (if known) <span class="text-small">(optional)</span></label>
                        <input type="text" name="bailiff_name" placeholder="e.g. Council Tax Bailiff, Enforcement agent name...">
                    </div>

                    <div class="form-group">
                        <label>Your message</label>
                        <textarea name="message" rows="3" placeholder="Tell us about your situation or ask a question..."></textarea>
                    </div>

                    <div class="privacy-note">
                        <i class="fas fa-lock"></i> By submitting you agree we may contact you via phone, email, WhatsApp, or SMS. Your data is treated per our Privacy Policy.
                    </div>

                    <input type="checkbox" name="botcheck" style="display: none;">
                    <button type="submit" id="submitBtn">📩 Send message</button>
                </form>
                <p class="text-small text-center" style="margin-top:1rem;">We work with trusted partners. Fees may apply.</p>
            </div>
        </div>

        <!-- ========== FOOTER ========== -->
        <footer>
            <div class="footer-nav">
                <a href="about.html"><i class="fa-regular fa-address-card"></i> About</a>
                <a href="terms.html"><i class="fa-regular fa-file-lines"></i> Terms</a>
                <a href="privacy.html"><i class="fa-regular fa-lock"></i> Privacy</a>
                <a href="contact.html"><i class="fa-regular fa-envelope"></i> Contact</a>
            </div>
            <div class="copyright">
                © 2025 Help with Bailiffs. All rights reserved. <br> 📞 Helpline: <strong>0203 996 3438</strong>
            </div>
        </footer>
    </div>

    <!-- JavaScript for hamburger and AJAX form -->
    <script>
        (function() {
            // Hamburger menu logic
            const hamburger = document.getElementById('hamburgerBtn');
            const menu = document.getElementById('navMenu');

            function toggleMenu() {
                menu.classList.toggle('show');
                hamburger.classList.toggle('active');
                const icon = hamburger.querySelector('i');
                const span = hamburger.querySelector('span');
                if (menu.classList.contains('show')) {
                    icon.className = 'fas fa-times';
                    span.textContent = 'Close';
                } else {
                    icon.className = 'fas fa-bars';
                    span.textContent = 'Menu';
                }
            }

            hamburger.addEventListener('click', function(e) {
                e.stopPropagation();
                toggleMenu();
            });

            menu.querySelectorAll('a').forEach(link => {
                link.addEventListener('click', function() {
                    if (window.innerWidth <= 699) toggleMenu();
                });
            });

            document.addEventListener('click', function(event) {
                if (window.innerWidth <= 699) {
                    if (!hamburger.contains(event.target) && !menu.contains(event.target) && menu.classList.contains('show')) {
                        toggleMenu();
                    }
                }
            });

            // ========== AJAX FORM SUBMISSION ==========
            const form = document.getElementById('contactForm');
            const thankyouDiv = document.getElementById('thankyouMessage');
            const submitBtn = document.getElementById('submitBtn');

            if (form) {
                form.addEventListener('submit', async function(e) {
                    e.preventDefault(); // No page reload

                    // Disable button
                    submitBtn.disabled = true;
                    submitBtn.textContent = 'Sending...';

                    const formData = new FormData(form);

                    try {
                        const response = await fetch('https://api.web3forms.com/submit', {
                            method: 'POST',
                            body: formData
                        });

                        const data = await response.json();

                        if (data.success) {
                            // Hide form, show thank you
                            form.style.display = 'none';
                            thankyouDiv.classList.remove('hidden');
                        } else {
                            alert('Something went wrong. Please try again or call us.');
                            submitBtn.disabled = false;
                            submitBtn.textContent = '📩 Send message';
                        }
                    } catch (error) {
                        alert('Network error. Please check your connection and try again.');
                        submitBtn.disabled = false;
                        submitBtn.textContent = '📩 Send message';
                    }
                });
            }
        })();
    </script>
</body>
</html>
